[project]
name = "anonymizer-engine"
version = "0.1.0"
description = "Local anonymization engine for Legal Anonymizer"
requires-python = ">=3.10"
dependencies = [
  "python-dateutil>=2.9.0"
]

[project.optional-dependencies]
spacy = [
  "spacy>=3.7.0"
]
